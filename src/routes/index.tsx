// Imports
import {NavigationContainer} from '@react-navigation/native';

// Imported Components
import {Stack} from './Stack';

// Functional Component
export const Route = () => {
  // Rendering
  return (
    <NavigationContainer>
      <Stack />
    </NavigationContainer>
  );
};
